const task16ID = 16;
const task16Name = 'Record Temperature';
const task16Loc = 'Located in Lab/Outside';
const task16Desc = 'Match the temperature number that the task displays on the right and click or hold on the arrows to increase or decrease the number that is currently shown on the left';
const task16TotalParts = 1;

const taskRecordTemperature = {
    id: task16ID,
    taskName: task16Name,
    taskLoc: task16Loc,
    taskDesc: task16Desc,
    taskTotalParts: task16TotalParts,
};

module.exports = {
    taskRecordTemperature,
};
